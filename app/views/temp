<p>
    <%= item.name %> - <%= item.temperature %> <%= item.temperatureUnit %><br />
    <%= item.probabilityOfPrecipitation.value %><% if (item.probabilityOfPrecipitation.unitCode == 'wmoUnit:percent') { %>% <% } %> chance of precipitation<br />
    <%= item.relativeHumidity.value %><% if (item.relativeHumidity.unitCode == 'wmoUnit:percent') { %>% <% } %> humidity<br />
    <%= item.windSpeed %> <%= item.windDirection %><br />
    <img src="<%= item.icon %>" /> <%=item.shortForecast %><br />
    <%= item.detailedForecast %>
  </p>


    <!-- <%= JSON.stringify(weekly) %> -->
    <!-- <img src="<%= weekly.icon %>" /> -->
    <!-- Slider main container -->

  <% if (locals.forecast && locals.forecast.length > 0) {
    //forecast.forEach((item) => {
  %>
      <%= JSON.stringify(forecast) %>
  <% //}); %>
  <hr>
  <%
    } else {
  %>
  <p>No forecast is available</p>
  <%
    }
  %>
